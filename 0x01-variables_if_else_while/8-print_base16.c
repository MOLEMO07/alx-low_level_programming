#include <stdio.h>
/**
 * main -prints all the numbers of base 16
 *
 * Return : 0
*/
void int main(void)
{
	int i;

	for (i = 0, i < 16, i++)
	{
		putchar(i);
	}
	for (i = 48, i < 58, i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
